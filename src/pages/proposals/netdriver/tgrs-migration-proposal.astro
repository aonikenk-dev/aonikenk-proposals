---
import ProposalLayout from '../../../layouts/ProposalLayout.astro';
import ProposalNav from '../../../components/ProposalNav.astro';
import ProposalCover from '../../../components/ProposalCover.astro';
import ProposalModule from '../../../components/ProposalModule.astro';
import ProposalTotals from '../../../components/ProposalTotals.astro';
import ProposalConditions from '../../../components/ProposalConditions.astro';
import ProposalSignatures from '../../../components/ProposalSignatures.astro';
import ProposalFooter from '../../../components/ProposalFooter.astro';

// ── Data ──────────────────────────────────────────────────────
import data from '../../../data/clients/netdriver/tgrs-migration-proposal.json';

// ── Auth guard ────────────────────────────────────────────────
const COOKIE_NAME = 'aonikenk_auth';
const slug = 'NETDRIVER_TGRS_MIGRATION_PROPOSAL';
const envKey    = `PROPOSAL_CODE_${slug}`;
const validCode = process.env[envKey] ?? import.meta.env[envKey] ?? process.env.PROPOSAL_CODE_DEFAULT ?? import.meta.env.PROPOSAL_CODE_DEFAULT ?? '';
const cookie = Astro.cookies.get(COOKIE_NAME);

if (!cookie || !validCode || cookie.value !== validCode) {
  return Astro.redirect(`/login?redirect=${Astro.url.pathname}`, 302);
}
// ─────────────────────────────────────────────────────────────

interface Item {
  name: string;
  desc: string;
  qty: string;
  price: number;
  show: boolean;
  exclude_from_subtotal?: boolean;
  exclude_from_total?: boolean;
}

const { meta, provider, client, intro, modules, totals, conditions } = data;

const pageTitle = `Presupuesto #${meta.ref} — ${client.name} · ${provider.name}${provider.tld}`;
const pageDescription = `Propuesta de ${meta.proposal_desc} para ${client.name}, preparada por ${provider.name}${provider.tld}.`;

let subTotal = 0;
let subTotalWithExtras = 0;
const totals_aux: {
  modules: {
    num: string;
    title: string;
    desc: string;
    table_title: string;
    items: Item[];
    subtotal_label: string;
    currency: string;
    subtotal: number;
    subtotal_first_month: number;
    subtotal_label_first_month?: string;
    notes?: string[];
  }[];
  subtotal: { label: string; value: number };
  extras: { label: string; value: number };
  clientBenefits: { label: string; value: number }[];
  subscriptionPlans: { label: string; description: string; value: number }[];
  subscriptionPlansNotes?: string[];
  discount: { label: string; percent: number };
  total: { label: string; value: number };
  notes?: string[];
} = {
  modules: [],
  subtotal: { label: totals.subtotal.label, value: 0 },
  extras: { label: totals.extras.label, value: totals.extras.value },
  clientBenefits: totals.clientBenefits,
  subscriptionPlans: totals.subscriptionPlans,
  subscriptionPlansNotes: totals.subscriptionPlansNotes,
  discount: totals.discount,
  total: totals.total,
  notes: totals.notes || [],
};

modules.map((mod) => {
  const subTotalModule = mod.items
    .filter(
      (item) =>
        item.show && !(item.exclude_from_subtotal || item.exclude_from_total),
    )
    .reduce((acc, item) => acc + item.price * parseInt(item.qty), 0);
  const subTotalFirstMonthModule = mod.items
    .filter((item) => item.show && !item.exclude_from_total)
    .reduce((acc, item) => acc + item.price * parseInt(item.qty), 0);
  subTotal += subTotalModule;
  subTotalWithExtras += subTotalFirstMonthModule;
  totals_aux.modules = [
    ...totals_aux.modules,
    {
      num: mod.num,
      title: mod.title,
      table_title: mod.table_title,
      desc: mod.desc,
      items: mod.items,
      subtotal_label: mod.subtotal_label,
      subtotal_label_first_month: mod.subtotal_label_first_month,
      currency: meta.currency,
      subtotal: subTotalModule,
      subtotal_first_month: subTotalFirstMonthModule,
      notes: mod.notes,
    },
  ];
});
---

<ProposalLayout title={pageTitle} description={pageDescription}>
  <ProposalNav
    name={provider.name}
    tld={provider.tld}
    ref={meta.ref}
    date={meta.date}
  />

  <ProposalCover
    provider={provider}
    client={client}
    meta={meta}
    totalValue={(
      subTotalWithExtras *
      (1 - totals_aux.discount.percent / 100)
    ).toFixed(2)}
  />

  <div id="content">
    <div class="p-inner">
      <!-- Intro -->
      <div class="s-header reveal">
        <div class="s-eyebrow"><span>Introducción</span></div>
        <h2 class="s-title">Contexto del proyecto</h2>
        <p class="s-desc">{intro}</p>
      </div>
      <div class="rule"></div>

      <!-- Plan and Estimation -->

      <div class="s-header reveal">
        <div class="s-eyebrow"><span>Plan y Estimación</span></div>
        <h2 class="s-title">Plan y Estimación</h2>
        <p class="s-desc">A continuación se presenta el plan detallado y las estimaciones para la migración del sistema actual a la nueva arquitectura. 
          Se muestra el análisis del stack actual, los puntos críticos a considerar, el código que se conservará y las implicaciones técnicas. </p>
      </div>

      <!-- ══ Notas ══ -->
      <div class="delta-box">
        <div class="delta-title">Notas de análisis</div>
        <p>
          <strong>1. El Excel tiene un esquema fijo de columnas (A→U)</strong> con reglas de negocio embebidas: columnas J+K+L conforman el <code>codFabrica</code>, si el chasis empieza con <code>8</code> es nacional (código 6801), si no es importado (6802).<br><br>
          <strong>2. Hay cuatro tablas de DB propias</strong> a alimentar secuencialmente: <code>gestor_autos</code> → <code>gestor_personas</code> → <code>gestor_tramites</code> → <code>gestor_titulares</code>. Con lógica de upsert en personas y autos (si existe el CUIT + id_gestor, usar el existente).<br><br>
          <strong>3. Existe la tabla <code>VE_apoderados</code></strong> que relaciona titular con apoderado (IDpersona_tit ↔ IDpersona_apo).<br><br>
          <strong>4. Hay un módulo de remitos</strong> no identificado antes: numeración correlativa, exportación en PDF y Excel, con cabecera (N°, fecha, cantidad) y cuerpo (chasis, certificado, titular, 01, 12).<br><br>
          <strong>5. Los PDFs de facturas y certificados</strong> se toman de un directorio local nombrados <code>Fchasis</code> y <code>Cchasi]</code>. No vienen del WS de TRGS — son archivos físicos del servidor que hay que servir.<br><br>
          <strong>6. La tabla <code>gestor_formularios</code></strong> registra los formularios 01, 01importado y 12 una vez que el WS responde OK.
        </p>
      </div>
      
      <!-- ══ 01 SISTEMA ACTUAL ══ -->
      <div class="s-header reveal">
        <div class="s-eyebrow"><span>01 — Sistema Actual</span></div>
        <h2 class="s-title">Stack Legacy (a reemplazar)</h2>
        <p class="s-desc">
          El sistema opera desde netdriver.com.ar con id_empresa 5452. El operador carga un Excel con columnas fijas A–U, el sistema alimenta cuatro tablas propias y luego envía el trámite al WS de TRGS (SUATS). 
          El sistema actual tiene dos capas de backend acopladas: ASP/VBScript
          construye el XML a partir de SQL Server y lo envía via HTTP POST a un
          PHP que sanitiza y llama al WS de TRGS. Los nodos marcados en Sienna
          se eliminan; el WS externo se conserva sin cambios.
        </p>
      </div>

        <div class="flow-row">
          <div class="flow-node dead">
            <div class="dead-badge">✕</div>
            <div class="fn-tag">Entrada</div>
            <div class="fn-name">Excel / Form</div>
            <div class="fn-tech">proceso manual</div>
          </div>
          <div class="flow-arrow">
            <div class="arrow-line"></div>
            <div class="arrow-label">HTTP POST XML</div>
          </div>
          <div class="flow-node dead">
            <div class="dead-badge">✕</div>
            <div class="fn-tag">Backend 1</div>
            <div class="fn-name">ASP / VBScript</div>
            <div class="fn-tech">SQL Server → XML</div>
          </div>
          <div class="flow-arrow">
            <div class="arrow-line"></div>
            <div class="arrow-label">POST input=XML</div>
          </div>
          <div class="flow-node dead">
            <div class="dead-badge">✕</div>
            <div class="fn-tag">Backend 2</div>
            <div class="fn-name">PHP</div>
            <div class="fn-tech">sanitize + SOAP</div>
          </div>
          <div class="flow-arrow">
            <div class="arrow-line"></div>
            <div class="arrow-label">SOAP 1.1</div>
          </div>
          <div class="flow-node ext">
            <div class="keep-badge">✓</div>
            <div class="fn-tag">Externo · intacto</div>
            <div class="fn-name">TRGS WS</div>
            <div class="fn-tech">trgs.com.ar</div>
          </div>
        </div>
        
        <div
          style="display:grid;grid-template-columns:repeat(3,1fr);gap:3px;margin-top:3px;margin-bottom:30px;"
        >
          <div
            style="background:var(--carbon);padding:22px 24px;border-top:2px solid var(--moss)"
          >
            <div
              style="font-size:10px;letter-spacing:0.2em;text-transform:uppercase;color:var(--moss);margin-bottom:10px"
            >
              Se conserva
            </div>
            <div style="font-size:11px;color:var(--muted);line-height:2">
              WS TRGS (trgs.com.ar)<br />Protocolo SOAP 1.1<br />Flujo eco →
              sesión → tramite<br />Credenciales de acceso<br />
              SQL Server (origen)<br/>
              Login NetDriver<br/>
              Lógica de negocio TRGS
            </div>
          </div>
          <div
            style="background:var(--carbon);padding:22px 24px;border-top:2px solid var(--sienna)"
          >
            <div
              style="font-size:10px;letter-spacing:0.2em;text-transform:uppercase;color:var(--sienna);margin-bottom:10px"
            >
              Se elimina
            </div>
            <div style="font-size:11px;color:var(--muted);line-height:2">
              ASP / VBScript (IIS)<br />PHP (apache)<br />Comunicación XML cruda<br />Acoplamiento ASP↔PHP
            </div>
          </div>          
          <div
            style="background:var(--carbon);padding:22px 24px;border-top:2px solid var(--ice)"
          >
            <div
              style="font-size:10px;letter-spacing:0.2em;text-transform:uppercase;color:var(--ice);margin-bottom:10px"
            >
              Se migra
            </div>
            <div style="font-size:11px;color:var(--muted);line-height:2">
              filterOperatorErrors() → JS<br />changeCedulaAzulArray() → JS<br/>Frontend → React<br />Parser
              XML → JSON nativo
            </div>
          </div>
        </div>

      <!-- ══ 01B EXCEL MAPPING ══ -->
      <div class="s-header reveal">
        <div class="s-eyebrow"><span>01b — Estructura del Excel</span></div>
          <h2 class="s-title">Mapeo de Columnas A–U</h2>
        <div class="s-desc">El Excel de entrada tiene un esquema de columnas fijo y conocido.</div>
      </div>

      <div class="excel-grid">
        <div class="excel-card">
          <div class="excel-card-header">Columnas A–J · Datos del vehículo y factura</div>
          <div class="excel-rows">
            <div class="excel-row"><div class="excel-col">A</div><div class="excel-field">facturaNro</div><div class="excel-table">gestor_autos</div></div>
            <div class="excel-row"><div class="excel-col">B</div><div class="excel-field">facturaFecha</div><div class="excel-table">gestor_autos</div></div>
            <div class="excel-row unused"><div class="excel-col">C</div><div class="excel-field">no se usa</div><div class="excel-table">—</div></div>
            <div class="excel-row"><div class="excel-col">D</div><div class="excel-field">nroChasis</div><div class="excel-table">gestor_autos</div></div>
            <div class="excel-row"><div class="excel-col">E</div><div class="excel-field">marcaChasis</div><div class="excel-table">gestor_autos</div></div>
            <div class="excel-row"><div class="excel-col">F</div><div class="excel-field">modelo</div><div class="excel-table">gestor_autos</div></div>
            <div class="excel-row"><div class="excel-col">G</div><div class="excel-field">nroMotor</div><div class="excel-table">gestor_autos</div></div>
            <div class="excel-row"><div class="excel-col">H</div><div class="excel-field">marcaMotor</div><div class="excel-table">gestor_autos</div></div>
            <div class="excel-row"><div class="excel-col">I</div><div class="excel-field">año</div><div class="excel-table">gestor_autos</div></div>
            <div class="excel-row"><div class="excel-col">J+K+L</div><div class="excel-field">codFabrica (concatenado)</div><div class="excel-table">tabla Autos</div></div>
          </div>
        </div>
        <div class="excel-card">
          <div class="excel-card-header">Columnas M–U · Titular, monto y datos extras</div>
          <div class="excel-rows">
            <div class="excel-row"><div class="excel-col">M</div><div class="excel-field">facturaMonto</div><div class="excel-table">gestor_autos</div></div>
            <div class="excel-row"><div class="excel-col">N</div><div class="excel-field">nombre titular</div><div class="excel-table">gestor_personas</div></div>
            <div class="excel-row"><div class="excel-col">O</div><div class="excel-field">CUIL/CUIT cliente</div><div class="excel-table">gestor_personas</div></div>
            <div class="excel-row unused"><div class="excel-col">P</div><div class="excel-field">no se usa</div><div class="excel-table">—</div></div>
            <div class="excel-row"><div class="excel-col">Q</div><div class="excel-field">certificadoFabrica</div><div class="excel-table">gestor_autos</div></div>
            <div class="excel-row unused"><div class="excel-col">R</div><div class="excel-field">no se usa</div><div class="excel-table">—</div></div>
            <div class="excel-row unused"><div class="excel-col">S</div><div class="excel-field">no se usa</div><div class="excel-table">—</div></div>
            <div class="excel-row unused"><div class="excel-col">T</div><div class="excel-field">no se usa</div><div class="excel-table">—</div></div>
            <div class="excel-row unused"><div class="excel-col">U</div><div class="excel-field">no se usa</div><div class="excel-table">—</div></div>
          </div>
        </div>
      </div>

      <div style="display:grid;grid-template-columns:repeat(3,1fr);gap:3px;margin-top:3px">
        <div style="background:var(--carbon);padding:20px 24px;border-left:2px solid var(--sienna)">
          <div style="font-size:10px;letter-spacing:0.18em;text-transform:uppercase;color:var(--sienna);margin-bottom:8px">Regla · Nacional/Importado</div>
          <div style="font-size:11px;color:var(--muted);line-height:2">Si nroChasis comienza con <span style="color:var(--body)">8</span> → Nacional → codigoClase <span style="color:var(--body)">6801</span><br>Si no → Importado → codigoClase <span style="color:var(--body)">6802</span></div>
        </div>
        <div style="background:var(--carbon);padding:20px 24px;border-left:2px solid var(--ice)">
          <div style="font-size:10px;letter-spacing:0.18em;text-transform:uppercase;color:var(--ice);margin-bottom:8px">Regla · codFabrica</div>
          <div style="font-size:11px;color:var(--muted);line-height:2">Columnas J + K + L se concatenan para formar <span style="color:var(--body)">codFabrica</span>. Si ya existe en tabla Autos → usar existente. Si no → insertar.</div>
        </div>
        <div style="background:var(--carbon);padding:20px 24px;border-left:2px solid var(--moss)">
          <div style="font-size:10px;letter-spacing:0.18em;text-transform:uppercase;color:var(--moss);margin-bottom:8px">Constantes de negocio</div>
          <div style="font-size:11px;color:var(--muted);line-height:2">
            tipoUso siempre <span style="color:var(--body)">1</span><br>
            id_tipo_persona siempre <span style="color:var(--body)">9</span> (jurídica: 0)<br>
            porcentaje titular siempre <span style="color:var(--body)">100%</span><br>
            id_empresa siempre <span style="color:var(--body)">5452</span>
          </div>
        </div>
      </div>

      <div class="rule"></div>

      <!-- ══ 02 NUEVO SISTEMA ══ -->
      <div class="s-header reveal">
        <div class="s-eyebrow"><span>02 — Nueva Arquitectura</span></div>
        <h2 class="s-title">Stack Propuesto</h2>
        <p class="s-desc">
          Se propone una nueva arquitectura desacoplada con un backend Node.js
          que expone una API REST para que la consuma el frontend React, y maneja la
          integración con el WS de TRGS. El nuevo sistema procesa archivos Excel
          de forma nativa, transformando los datos a JSON y comunicándose con
          el WS de TRGS mediante SOAP. La migración se realizará en paralelo al
          sistema actual, asegurando la continuidad operativa. El WS de TRGS sigue siendo el destino final; solo cambia quién lo llama.
        </p>
      </div>

      <div class="arch-grid">

    <!-- REACT -->
    <div class="arch-layer react-layer">
      <div class="layer-tag">Frontend</div>
      <div class="layer-name c-react">React.js + Vite</div>
      <div class="layer-items">
        <div class="layer-item">ExcelUpload — drag &amp; drop<div class="li-sub">SheetJS · columnas A–U · preview pre-envío</div></div>
        <div class="layer-item">ListadoTrámites<div class="li-sub">id, titular, chasis, nacional/importado, estado</div></div>
        <div class="layer-item">InputFormulario<div class="li-sub">campo para n° form 01 / 01importado / 12</div></div>
        <div class="layer-item">BotónEnviarSUATS<div class="li-sub">dispara llamada al WS · feedback de estado</div></div>
        <div class="layer-item">MóduloRemitos<div class="li-sub">check para seleccionar · genera PDF + Excel</div></div>
        <div class="layer-item">VisorPDFs<div class="li-sub">01, 12, facturas, certificados, enmienda, DDJJ</div></div>
        <div class="layer-item">Dashboard<div class="li-sub">historial, traID, PDFs</div></div>
      </div>
    </div>

    <!-- ARROW -->
    <div class="arch-connector">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
        <path d="M5 12H19M19 12L13 6M19 12L13 18" stroke="#C0623A" stroke-width="1.5" stroke-linecap="round"/>
      </svg>
    </div>

    <!-- NODE -->
    <div class="arch-layer node-layer">
      <div class="layer-tag">Backend</div>
      <div class="layer-name c-node">Node.js + Express</div>
      <div class="layer-items">
        <div class="layer-item">POST /api/excel/import<div class="li-sub">excelParser → insert gestor_autos + gestor_personas</div></div>
        <div class="layer-item">POST /api/tramites/enviar<div class="li-sub">sanitize → llamada SOAP a TRGS → update gestor_tramites</div></div>
        <div class="layer-item">GET /api/tramites/:id/formulario<div class="li-sub">obtener_formularios → gestor_formularios</div></div>
        <div class="layer-item">POST /api/tramites/:id/cancelar<div class="li-sub">cancelar_tramite_01</div></div>
        <div class="layer-item">GET /api/pdfs/:tipo/:chasis<div class="li-sub">sirve Fchasis o Cchasis del directorio local</div></div>
        <div class="layer-item">POST /api/remitos<div class="li-sub">genera PDF + Excel del remito correlativo</div></div>
        <div class="layer-item">sanitize.js<div class="li-sub">filterOperatorErrors migrado a JS</div></div>
        <div class="layer-item">trgsService.js<div class="li-sub">eco → abrir_sesion → op → cerrar</div></div>
        <div class="layer-item">excelParser.js<div class="li-sub">SheetJS → objeto tramite</div></div>
      </div>
    </div>

    <!-- ARROW -->
    <div class="arch-connector">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
        <path d="M5 12H19M19 12L13 6M19 12L13 18" stroke="#C0623A" stroke-width="1.5" stroke-linecap="round"/>
      </svg>
    </div>

    <!-- DB -->
    <div class="arch-layer db-layer">
      <div class="layer-tag">Persistencia</div>
      <div class="layer-name c-db">SQL Server</div>
      <div class="layer-items">
        <div class="layer-item">gestor_autos</div>
        <div class="layer-item">gestor_personas<div class="li-sub">upsert por CUIT + id_gestor</div></div>
        <div class="layer-item">VE_apoderados<div class="li-sub">tit ↔ apo</div></div>
        <div class="layer-item">gestor_tramites<div class="li-sub">estado + traID</div></div>
        <div class="layer-item">gestor_titulares</div>
        <div class="layer-item">gestor_formularios<div class="li-sub">post-WS OK</div></div>
        <div class="layer-item">remitos<div class="li-sub">numeración correlativa</div></div>
      </div>
    </div>

    <!-- ARROW -->
    <div class="arch-connector">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
        <path d="M5 12H19M19 12L13 6M19 12L13 18" stroke="#C0623A" stroke-width="1.5" stroke-linecap="round"/>
      </svg>
    </div>

    <!-- TRGS -->
    <div class="arch-layer ext-layer">
      <div class="layer-tag">Externo · sin cambios</div>
      <div class="layer-name c-ext">TRGS Web Service</div>
      <div class="layer-items">
        <div class="layer-item">eco()<div class="li-sub">verificar disponibilidad</div></div>
        <div class="layer-item">abrir_sesion()<div class="li-sub">retorna ingID</div></div>
        <div class="layer-item">generar_tramite_01()<div class="li-sub">retorna traID</div></div>
        <div class="layer-item">obtener_formularios()<div class="li-sub">PDF base64</div></div>
        <div class="layer-item">cancelar_tramite_01()</div>
        <div class="layer-item">cerrar_sesion()</div>
      </div>
    </div>

      </div>

      <div class="rule"></div>

      <!-- ══ 03 DATA FLOW ══ -->
      <div class="s-header reveal">
        <div class="s-eyebrow"><span>03 — Flujo de Datos</span></div>
        <h2 class="s-title">Paso a paso</h2>
        <p class="s-desc">
          Recorrido completo de un trámite desde que el usuario carga el Excel hasta que recibe el traID confirmado y descarga el PDF.
        </p>
      </div>

      <div class="dataflow">
    <div class="df-step">
      <div class="df-left">
        <div class="df-step-num">01</div>
        <div class="df-actor" style="color:#61dafb">React</div>
      </div>
      <div class="df-right">
        <div class="df-action">Usuario carga el Excel (drag & drop o selección)</div>
        <div class="df-detail">Se toma el archivo Excel y se parsean algunos campos importantes.</div>
      </div>
    </div>
    <div class="df-step">
      <div class="df-left">
        <div class="df-step-num">02</div>
        <div class="df-actor" style="color:#61dafb">React</div>
      </div>
      <div class="df-right">
        <div class="df-action">Validación en el frontend antes de enviar</div>
        <div class="df-detail">CUIT, DNI, email, CP requeridos → preview de los datos → confirmación del operador → upload del archivo</div>
      </div>
    </div>
    <div class="df-step">
      <div class="df-left">
        <div class="df-step-num">03</div>
        <div class="df-actor" style="color:var(--moss)">Node.js</div>
      </div>
      <div class="df-right">
        <div class="df-action">POST /api/excel/import — guarda en DB con estado "pendiente"</div>
        <div class="df-detail">Se parsea el archivo Excel y se procesa cada fila  → Inserts en tablas correspondientes   → El trámite queda en estado pendiente hasta que el operador dispare el envío</div>
      </div>
    </div>
    <div class="df-step">
      <div class="df-left"><div class="df-step-num">04</div><div class="df-actor" style="color:#61dafb">React</div></div>
      <div class="df-right">
        <div class="df-action">Listado de trámites · operador ingresa n° de formulario 01 / 12 y presiona ENVIAR SUATS</div>
        <div class="df-detail">id_tramite · titular · chasis · nacional o importado · input n° form · botón enviar · check remito</div>
      </div>
    </div>
    <div class="df-step">
      <div class="df-left">
        <div class="df-step-num">05</div>
        <div class="df-actor" style="color:var(--moss)">Node.js</div>
      </div>
      <div class="df-right">
        <div class="df-action">sanitize.js — limpia y normaliza todos los campos</div>
        <div class="df-detail">tel → solo números · CUIT/doc → parseFloat · email → trim/sin acentos · ocupación → solo letras · CP → solo números</div>
      </div>
    </div>
    <div class="df-step">
      <div class="df-left">
        <div class="df-step-num">06</div>
        <div class="df-actor" style="color:var(--moss)">Node.js</div>
      </div>
      <div class="df-right">
        <div class="df-action">trgsService.js — eco() → abrir_sesion() → generar_tramite_01()</div>
        <div class="df-detail">node-soap · SOAP 1.1 · https://www.trgs.com.ar:443/service/index.php?wsdl</div>
      </div>
    </div>
    <div class="df-step">
      <div class="df-left">
        <div class="df-step-num">07</div>
        <div class="df-actor" style="color:var(--sienna)">TRGS WS</div>
      </div>
      <div class="df-right">
        <div class="df-action">TRGS procesa el trámite y devuelve traID</div>
        <div class="df-detail">rspID = 1 → éxito · rspID &lt; 0 → trámite queda pendiente con leyenda de error</div>
      </div>
    </div>
    <div class="df-step">
      <div class="df-left">
        <div class="df-step-num">07</div>
        <div class="df-actor" style="color:var(--moss)">Node.js</div>
      </div>
      <div class="df-right">
        <div class="df-action">Guarda traID, actualiza estado → "completado" · cerrar_sesion()</div>
        <div class="df-detail">Descarga PDFs: 01 / 01importado / 12 / DDJJ / Enmienda vienen del WS · Facturas y Certificados desde directorio local Fchasis / Cchasis</div>
      </div>
    </div>
    <div class="df-step">
      <div class="df-left">
        <div class="df-step-num">08</div>
        <div class="df-actor" style="color:#61dafb">React</div>
      </div>
      <div class="df-right">
        <div class="df-action">Dashboard muestra traID confirmado + listado de PDFs para imprimir disponible en pantalla</div>
        <div class="df-detail">GET /api/tramites/:id/formulario?tipo=F01 | 01 / 01importado · 12 · Facturas · Certificados · Enmienda · DDJJ → PDF base64 → descarga</div>
      </div>
    </div>
    <div class="df-step">
      <div class="df-left"><div class="df-step-num">09</div><div class="df-actor" style="color:var(--moss)" style="color:var(--moss)">Node.js</div></div>
      <div class="df-right">
        <div class="df-action">Generación de remito — numeración correlativa automática</div>
        <div class="df-detail new">POST /api/remitos · cabecera: N°, fecha, cant. trámites · cuerpo: chasis, certificado, titular, 01, 12 · salida: PDF + Excel</div>
      </div>
    </div>
      </div>
      
      <div class="rule"></div>

      <!-- ══ 04 ESTIMACIÓN ══ -->
      <div class="s-header reveal">
        <div class="s-eyebrow"><span>04 — Estimación</span></div>
        <h2 class="s-title">Horas por Módulo</h2>
        <p class="s-desc">
          Desglose por área con las tareas concretas identificadas a partir del análisis del código existente (ASP, PHP, XMLs de prueba y manual del WS).
        </p>
      </div>

<div class="est-grid">

    <!-- BACKEND -->
    <div class="est-card">
      <div class="est-header">
        <div class="est-header-title">Backend — Node.js + Express</div>
        <div class="est-header-color" style="background:var(--moss)"></div>
      </div>
      <div class="est-rows">
        <div class="est-row">
          <div class="est-task">Setup proyecto, estructura, configuración<span class="task-sub">Express, cors, logger, estructura de carpetas</span></div>
          <div class="est-hours" style="color:var(--moss)">2 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">Modelos DB + migraciones (Sequelize/Prisma)<span class="task-sub">tramites, titulares, vehiculos, apoderados, cedulas</span></div>
          <div class="est-hours" style="color:var(--moss)">4 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">trgsService.js — cliente SOAP completo<span class="task-sub">eco, abrir_sesion, generar, obtener_formularios, cancelar, cerrar</span></div>
          <div class="est-hours" style="color:var(--moss)">8 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">sanitize.js — migración de filterOperatorErrors<span class="task-sub">todas las reglas del PHP + casos edge identificados</span></div>
          <div class="est-hours" style="color:var(--moss)">6 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">API routes: POST /tramites, GET /formulario, POST /cancelar<span class="task-sub">validación, manejo de errores TRGS, respuestas legibles</span></div>
          <div class="est-hours" style="color:var(--moss)">2 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">Servir PDFs locales Fchasis / Cchasis<span class="task-sub">GET /api/pdfs/:tipo/:chasis · directorio configurable</span></div>
          <div class="est-hours" style="color:var(--moss)">4 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">Módulo remitos — generación PDF + Excel<span class="task-sub">numeración correlativa · cabecera + cuerpo · exportación dual</span></div>
          <div class="est-hours" style="color:var(--moss)">6 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">excelParser.js — mapeo columnas → objeto tramite<span class="task-sub">SheetJS, múltiples secciones, validación de columnas</span></div>
          <div class="est-hours" style="color:var(--moss)">6 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">Testing — servicios críticos (Jest)<span class="task-sub">sanitize unit tests, SOAP mock, casos GH/apoderados/condominio</span></div>
          <div class="est-hours" style="color:var(--moss)">4 hs</div>
        </div>
      </div>
      <div class="est-subtotal">
        <div class="est-sub-label">Subtotal Backend</div>
        <div class="est-sub-val" style="color:var(--moss)">42 hs</div>
      </div>
    </div>

    <!-- FRONTEND -->
    <div class="est-card">
      <div class="est-header">
        <div class="est-header-title">Frontend — React + Vite</div>
        <div class="est-header-color" style="background:#61dafb"></div>
      </div>
      <div class="est-rows">
        <div class="est-row">
          <div class="est-task">Setup, routing (React Router), estado global<span class="task-sub">Vite, Zustand/Context, axios, estructura de módulos</span></div>
          <div class="est-hours" style="color:#61dafb">4 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">Design system / algunos componentes base<span class="task-sub">Layout, estilos visuales, Input, Select, Button, Card, notificaciones</span></div>
          <div class="est-hours" style="color:#61dafb">4 hs</div>
        </div>
        <!-- <div class="est-row">
          <div class="est-task">FormDatosDelTramite + FormVehiculo<span class="task-sub">todos los campos de trgDatos + trgVehiculo</span></div>
          <div class="est-hours" style="color:#61dafb">8 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">FormTitulares — lista dinámica<span class="task-sub">agregar/quitar titulares, cónyuge condicional, validaciones</span></div>
          <div class="est-hours" style="color:#61dafb">10 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">FormApoderados + FormCedulasAzul + FormGuardaHabitual<span class="task-sub">arrays dinámicos, campos condicionales, tablas de códigos</span></div>
          <div class="est-hours" style="color:#61dafb">10 hs</div>
        </div> -->
        <div class="est-row">
          <div class="est-task">ExcelUpload — drag &amp; drop + previsualización<span class="task-sub">SheetJS, mapeo a secciones, detección de errores de columnas</span></div>
          <div class="est-hours" style="color:#61dafb">4 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">ListadoTrámites — pantalla principal del operador<span class="task-sub">tabla: id, titular, chasis, nac/imp, estado · input n° form · botón ENVIAR SUATS</span></div>
          <div class="est-hours" style="color:#61dafb">4 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">VisorPDFs — listado para imprimir<span class="task-sub">01 / 01imp · 12 · facturas · certificados · enmienda · DDJJ</span></div>
          <div class="est-hours" style="color:#61dafb">5 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task new-task">MóduloRemitos — selección + generación<span class="task-sub">check por trámite · vista previa · descarga PDF + Excel</span></div>
          <div class="est-hours" style="color:#61dafb">6 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">Dashboard — historial de trámites (PROPUESTA)<span class="task-sub">tabla paginada, estados, descarga F01 y F12</span></div>
          <div class="est-hours" style="color:#61dafb">6 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">Manejo de errores UI + loading states<span class="task-sub">errores TRGS legibles, retry, feedback visual al operador</span></div>
          <div class="est-hours" style="color:#61dafb">3 hs</div>
        </div>
      </div>
      <div class="est-subtotal">
        <div class="est-sub-label">Subtotal Frontend</div>
        <div class="est-sub-val" style="color:#61dafb">36 hs</div>
      </div>
    </div>

    <!-- INFRA -->
    <div class="est-card">
      <div class="est-header">
        <div class="est-header-title">Infraestructura &amp; Deploy (de ser necesario alguna modificación o agregado)</div>
        <div class="est-header-color" style="background:var(--ice)"></div>
      </div>
      <div class="est-rows">
        <div class="est-row">
          <div class="est-task">Setup SQL Server <span class="task-sub">entorno local dev + producción</span></div>
          <div class="est-hours" style="color:var(--ice)">4 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">Variables de entorno, secrets, configuración prod<span class="task-sub">credenciales TRGS, DB, CORS, HTTPS</span></div>
          <div class="est-hours" style="color:var(--ice)">3 hs</div>
        </div>
        <!-- <div class="est-row">
          <div class="est-task">Docker / PM2 + proxy inverso (nginx o Caddy)<span class="task-sub">según hosting elegido</span></div>
          <div class="est-hours" style="color:var(--ice)">6 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">CI/CD básico (GitHub Actions)</div>
          <div class="est-hours" style="color:var(--ice)">4 hs</div>
        </div> -->
      </div>
      <div class="est-subtotal">
        <div class="est-sub-label">Subtotal Infra</div>
        <div class="est-sub-val" style="color:var(--ice)">7 hs</div>
      </div>
    </div>

    <!-- QA -->
    <div class="est-card">
      <div class="est-header">
        <div class="est-header-title">QA, Integridad de Datos &amp; UAT</div>
        <div class="est-header-color" style="background:var(--sienna)"></div>
      </div>
      <div class="est-rows">
        <div class="est-row">
          <div class="est-task">Pruebas contra entorno testing TRGS<span class="task-sub">testingvdc.trgs.com.ar · casos GH, apoderados, condominio</span></div>
          <div class="est-hours" style="color:var(--sienna)">3 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">Integridad de datos SQL Server<span class="task-sub">scripts de migración (de ser necesarios), validación de integridad de datos</span></div>
          <div class="est-hours" style="color:var(--sienna)">2 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">UAT con usuarios finales + correcciones<span class="task-sub">Feedback con operadores reales (Gabi 😄)</span></div>
          <div class="est-hours" style="color:var(--sienna)">1 hs</div>
        </div>
        <div class="est-row">
          <div class="est-task">Documentación técnica<span class="task-sub">README, variables de entorno, guía de deploy</span></div>
          <div class="est-hours" style="color:var(--sienna)">5 hs</div>
        </div>
      </div>
      <div class="est-subtotal">
        <div class="est-sub-label">Subtotal QA / Migración</div>
        <div class="est-sub-val" style="color:var(--sienna)">11 hs</div>
      </div>
    </div>

  </div>

  <!-- CHART -->
  <div class="chart-block">
    <div class="chart-label">// distribución de horas por área</div>
    <div class="chart-bars">
      <div class="chart-row">
        <div class="chart-row-label">Frontend (React)</div>
        <div class="chart-track"><div class="chart-fill" style="width:75%;background:#61dafb"></div></div>
        <div class="chart-val">36 hs</div>
      </div>
      <div class="chart-row">
        <div class="chart-row-label">Backend (Node.js)</div>
        <div class="chart-track"><div class="chart-fill" style="width:90%;background:var(--moss)"></div></div>
        <div class="chart-val">42 hs</div>
      </div>
      <div class="chart-row">
        <div class="chart-row-label">QA / Migración / UAT</div>
        <div class="chart-track"><div class="chart-fill" style="width:53%;background:var(--sienna)"></div></div>
        <div class="chart-val">11 hs</div>
      </div>
      <div class="chart-row">
        <div class="chart-row-label">Infra / Deploy</div>
        <div class="chart-track"><div class="chart-fill" style="width:24%;background:var(--ice)"></div></div>
        <div class="chart-val">7 hs</div>
      </div>
    </div>
  </div>

  <!-- TOTAL -->
  <div class="total-bar">
    <div>
      <div class="total-label">// Total estimado</div>
      <div class="total-range">90 – 100 <span>horas</span></div>
    </div>
    <div class="total-breakdown">
      <div class="tb-item">
        <div class="tb-val" style="color:#61dafb">42</div>
        <div class="tb-lbl">Frontend</div>
      </div>
      <div class="tb-item">
        <div class="tb-val" style="color:var(--moss)">36</div>
        <div class="tb-lbl">Backend</div>
      </div>
      <div class="tb-item">
        <div class="tb-val" style="color:var(--sienna)">11</div>
        <div class="tb-lbl">QA + Migración</div>
      </div>
      <div class="tb-item">
        <div class="tb-val" style="color:var(--ice)">7</div>
        <div class="tb-lbl">Infra</div>
      </div>
    </div>
    <div class="total-equiv">
      <div class="total-label">// Equivale a</div>
      <div class="eq-val">2 – 3 semanas</div>
      <div class="eq-sub">a dedicación full-time (8 hs/día)</div>
    </div>
  </div>

  <!-- NOTES -->
  <div class="notes-grid">
    <div class="note-card warn">
      <div class="note-icon">⚠</div>
      <div class="note-title">Variable: el Excel</div>
      <div class="note-body">Si hay múltiples formatos posibles del Excel, el mapeo de columnas puede ser más complejo y sumar hasta hs adicionales al desarrollo.</div>
    </div>
    <div class="note-card warn">
      <div class="note-icon">⚠</div>
      <div class="note-title">Integridad y migración de datos</div>
      <div class="note-body">Las 11 hs asumen un esquema SQL Server relativamente limpio y pocos o nulos datos a migrar. Si hay algún tipo de inconsistencias, puede incrementarse el tiempo de desarrollo.</div>
    </div>
    <div class="note-card info">
      <div class="note-icon">—</div>
      <div class="note-title">No incluido</div>
      <div class="note-body">Autenticación/login de usuarios, roles y permisos, reportes avanzados, notificaciones por email. Cada módulo involucra nuevas estimaciones si se requieren en el alcance.</div>
    </div>
  </div>

      <div class="rule"></div>

      <!-- Proposal -->

      <div class="s-header reveal">
        <div class="s-eyebrow"><span>Presupuesto</span></div>
        <h2 class="s-title">Presupuesto</h2>
        <p class="s-desc">Presentación del presupuesto, dividido por módulos y el valor de cada uno de acuerdo a complejidad.</p>
      </div>

      <!-- Modules -->
      {
        totals_aux.modules.map((mod) => (
          <ProposalModule
            num={mod.num}
            title={mod.title}
            desc={mod.desc}
            table_title={mod.table_title}
            items={mod.items}
            currency={meta.currency}
            subtotal={mod.subtotal}
            subtotal_first_month={mod.subtotal_first_month}
            subtotal_label={mod.subtotal_label}
            subtotal_label_first_month={mod.subtotal_label_first_month}
            notes={mod.notes}
          />
        ))
      }

      <!-- Totals -->
      <ProposalTotals
        totals={{
          ...totals_aux,
          extras: {
            label: totals_aux.extras.label,
            value: subTotalWithExtras - subTotal,
          },
          clientBenefits: totals_aux.clientBenefits,
          subscriptionPlans: totals_aux.subscriptionPlans,
          subscriptionPlansNotes: totals_aux.subscriptionPlansNotes,
          subtotal: { label: totals_aux.subtotal.label, value: subTotal },
          total: {
            label: totals_aux.total.label,
            value: subTotalWithExtras * (1 - totals_aux.discount.percent / 100),
          },
        }}
        currency={meta.currency}
      />
      <ProposalConditions conditions={conditions} />
      <ProposalSignatures provider={provider} client={client} />
    </div>
  </div>

  <ProposalFooter provider={provider} client={client} meta={meta} />
</ProposalLayout>

<style lang="scss">
  @use '../../../styles/tokens' as *;
  #content {
    padding: 0 0 100px;
    background: var(--obsidian);
  }

  /* ── FLOW (legacy) ── */
  .flow-row {
    display: flex; align-items: center; gap: 0;
    overflow-x: auto; padding: 8px 0 24px;
    margin-top: 30px;
  }
  .flow-node {
    flex-shrink: 0;
    background: var(--carbon);
    border: 1px solid var(--border);
    padding: 18px 22px;
    min-width: 136px;
    text-align: center;
    position: relative;
  }
  .flow-node .fn-tag {
    font-size: 9px; letter-spacing: 0.2em; text-transform: uppercase;
    color: var(--muted); margin-bottom: 6px;
  }
  .flow-node .fn-name {
    font-family: 'Syne', sans-serif; font-weight: 700;
    font-size: 13px; color: var(--white); margin-bottom: 4px;
  }
  .flow-node .fn-tech { font-size: 10px; color: var(--muted); }
  .flow-node.dead { border-color: rgba(192,98,58,0.35); }
  .flow-node.dead .fn-name { color: rgba(192,98,58,0.8); }
  .flow-node.dead .fn-tech { color: var(--muted); }
  .flow-node.ext  { border-color: rgba(123,175,196,0.35); background: var(--slate); }
  .flow-node.ext  .fn-name { color: var(--ice); }

  .dead-badge {
    position: absolute; top: -9px; right: -9px;
    width: 20px; height: 20px;
    background: var(--sienna); border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 10px; font-weight: 700; color: var(--white);
  }
  .keep-badge {
    position: absolute; top: -9px; right: -9px;
    width: 20px; height: 20px;
    background: var(--moss); border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 10px; font-weight: 700; color: var(--white);
  }

  .flow-arrow {
    flex-shrink: 0;
    display: flex; flex-direction: column; align-items: center;
    padding: 0 6px; gap: 4px;
  }
  .arrow-line {
    width: 44px; height: 1px; background: var(--border);
    position: relative;
  }
  .arrow-line::after {
    content: '';
    position: absolute; right: -1px; top: -4px;
    border-left: 6px solid var(--border);
    border-top: 4px solid transparent;
    border-bottom: 4px solid transparent;
  }
  .arrow-label { font-size: 9px; color: var(--muted); text-align: center; max-width: 72px; letter-spacing: 0.04em; }

   .arch-grid {
    display: grid;
    grid-template-columns: 1fr 32px 1fr 32px 1fr 32px 1fr;
    gap: 3px;
    align-items: start;
    margin-bottom: 30px;
  }
  .arch-layer {
    background: var(--carbon);
    border-top: 2px solid var(--border);
    padding: 24px;
  }
  .arch-layer.react-layer  { border-top-color: #61dafb; }
  .arch-layer.node-layer   { border-top-color: var(--moss); }
  .arch-layer.db-layer     { border-top-color: var(--ice); }
  .arch-layer.ext-layer    { border-top-color: var(--sienna); }

  .layer-tag { font-size: 9px; letter-spacing: 0.22em; text-transform: uppercase; color: var(--muted); margin-bottom: 8px; }
  .layer-name {
    font-family: 'Syne', sans-serif; font-weight: 700;
    font-size: 15px; color: var(--white); margin-bottom: 16px;
    letter-spacing: -0.02em;
  }
  .layer-name.c-react  { color: #61dafb; }
  .layer-name.c-node   { color: var(--moss); }
  .layer-name.c-db     { color: var(--ice); }
  .layer-name.c-ext    { color: var(--sienna); }

  .layer-items { display: flex; flex-direction: column; gap: 3px; }
  .layer-item {
    background: var(--slate);
    border: 1px solid var(--border);
    padding: 8px 12px;
    font-size: 11px; color: var(--body); line-height: 1.5;
  }
  .layer-item .li-sub { font-size: 10px; color: var(--muted); margin-top: 2px; }

  .arch-connector {
    display: flex; align-items: center; justify-content: center;
    padding-top: 70px;
  }

  /* ── DATAFLOW ── */
  .dataflow { display: flex; flex-direction: column; margin-bottom: 30px; }

  .df-step {
    display: grid;
    grid-template-columns: 148px 1fr;
  }
  .df-left {
    padding: 18px 20px 18px 0;
    display: flex; flex-direction: column; align-items: flex-end; gap: 4px;
    border-right: 1px solid var(--border);
    position: relative;
  }
  .df-left::after {
    content: '';
    position: absolute; right: -5px; top: 22px;
    width: 9px; height: 9px; border-radius: 50%;
    background: var(--obsidian); border: 1px solid var(--muted);
  }
  .df-step:hover .df-left::after { background: var(--sienna); border-color: var(--sienna); }

  .df-step-num { font-size: 9px; letter-spacing: 0.18em; color: var(--muted); }
  .df-actor {
    font-family: 'Syne', sans-serif; font-weight: 600;
    font-size: 11px; color: var(--subtle); text-align: right;
  }
  .df-right { padding: 18px 0 18px 24px; }
  .df-action { font-size: 13px; color: var(--white); margin-bottom: 6px; font-family: 'Syne', sans-serif; font-weight: 600; letter-spacing: -0.01em; }
  .df-detail {
    font-size: 10px; color: var(--muted);
    background: var(--carbon); border: 1px solid var(--border);
    padding: 4px 10px; display: inline-block; letter-spacing: 0.02em;
  }

  /* ── ESTIMATION ── */
  .est-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 3px; }

  .est-card { background: var(--carbon); }
  .est-header {
    padding: 16px 22px;
    border-bottom: 1px solid var(--border);
    display: flex; align-items: center; justify-content: space-between;
  }
  .est-header-title {
    font-family: 'Syne', sans-serif; font-weight: 700;
    font-size: 13px; color: var(--white); letter-spacing: -0.01em;
  }
  .est-header-color { width: 8px; height: 8px; border-radius: 50%; }

  .est-rows { padding: 4px 0; }
  .est-row {
    display: grid; grid-template-columns: 1fr auto;
    gap: 12px; padding: 10px 22px;
    border-bottom: 1px solid var(--border);
    align-items: center;
  }
  .est-row:last-child { border-bottom: none; }
  .est-task { font-size: 12px; color: var(--body); line-height: 1.5; }
  .est-task .task-sub { font-size: 10px; color: var(--muted); display: block; margin-top: 2px; }
  .est-hours {
    font-family: 'DM Mono', monospace; font-size: 11px; font-weight: 500;
    color: var(--white); white-space: nowrap;
    background: var(--slate); border: 1px solid var(--border);
    padding: 3px 10px; min-width: 52px; text-align: center;
  }

  .est-subtotal {
    display: grid; grid-template-columns: 1fr auto;
    padding: 14px 22px;
    background: var(--slate); border-top: 1px solid var(--border);
  }
  .est-sub-label { font-size: 10px; color: var(--muted); letter-spacing: 0.15em; text-transform: uppercase; font-family: 'Syne', sans-serif; font-weight: 600; }
  .est-sub-val { font-family: 'DM Mono', monospace; font-size: 14px; font-weight: 500; }

  /* ── CHART ── */
  .chart-block {
    background: var(--carbon);
    padding: 28px 32px;
    margin-top: 3px;
  }
  .chart-label { font-size: 10px; letter-spacing: 0.22em; text-transform: uppercase; color: var(--muted); margin-bottom: 24px; }
  .chart-bars { display: flex; flex-direction: column; gap: 14px; }
  .chart-row { display: grid; grid-template-columns: 176px 1fr 52px; gap: 16px; align-items: center; }
  .chart-row-label { font-size: 11px; color: var(--body); text-align: right; }
  .chart-track { height: 6px; background: var(--slate); }
  .chart-fill { height: 100%; }
  .chart-val { font-family: 'DM Mono', monospace; font-size: 10px; color: var(--muted); text-align: right; }

  /* ── TOTAL ── */
  .total-bar {
    background: var(--carbon); border-top: 2px solid var(--sienna);
    padding: 28px 32px;
    display: flex; align-items: center; justify-content: space-between;
    flex-wrap: wrap; gap: 24px;
    margin-top: 3px;
  }
  .total-label { font-size: 10px; letter-spacing: 0.22em; text-transform: uppercase; color: var(--muted); margin-bottom: 6px; }
  .total-range {
    font-family: 'Syne', sans-serif; font-weight: 800;
    font-size: 36px; color: var(--white); letter-spacing: -0.03em; line-height: 1;
  }
  .total-range span { color: var(--sienna); font-size: 20px; font-weight: 400; font-family: 'DM Mono', monospace; }
  .total-breakdown { display: flex; gap: 28px; flex-wrap: wrap; }
  .tb-item { text-align: center; }
  .tb-val { font-family: 'Syne', sans-serif; font-weight: 700; font-size: 22px; color: var(--white); line-height: 1; }
  .tb-lbl { font-size: 9px; color: var(--muted); letter-spacing: 0.15em; text-transform: uppercase; margin-top: 4px; }
  .total-equiv { text-align: right; }
  .total-equiv .eq-val {
    font-family: 'DM Mono', monospace; font-size: 16px; color: var(--subtle);
  }
  .total-equiv .eq-sub { font-size: 10px; color: var(--muted); margin-top: 4px; letter-spacing: 0.06em; }

  /* ── NOTES ── */
  .notes-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 3px; margin-top: 3px; margin-bottom: 30px; }
  .note-card { background: var(--carbon); padding: 24px 28px; }
  .note-icon { font-size: 18px; margin-bottom: 10px; }
  .note-title { font-family: 'Syne', sans-serif; font-weight: 700; font-size: 13px; color: var(--white); margin-bottom: 8px; }
  .note-body { font-size: 11px; color: var(--muted); line-height: 1.9; }
  .note-card.warn { border-top: 2px solid var(--sienna); }
  .note-card.info { border-top: 2px solid var(--moss); }

  /* ── DELTA BOX ── */
  .delta-box { background:var(--carbon); border-left:2px solid var(--sienna); padding:20px 24px; margin-bottom:40px; }
  .delta-box .delta-title { font-family:'Syne',sans-serif; font-weight:700; font-size:12px; color:var(--sienna); letter-spacing:0.08em; text-transform:uppercase; margin-bottom:8px; }
  .delta-box p { font-size:12px; color:var(--muted); line-height:1.9; }
  .delta-box p strong { color:var(--body); }

  /* ── EXCEL MAPPING ── */
  .excel-grid { display:grid; grid-template-columns:repeat(2,1fr); gap:3px; }
  .excel-card { background:var(--carbon); }
  .excel-card-header { padding:12px 18px; border-bottom:1px solid var(--border); border-top:2px solid var(--moss); font-family:'Syne',sans-serif; font-weight:700; font-size:12px; color:var(--white); }
  .excel-rows { padding:4px 0; }
  .excel-row { display:grid; grid-template-columns:28px 1fr auto; gap:10px; padding:7px 18px; border-bottom:1px solid var(--border); align-items:center; font-size:11px; }
  .excel-row:last-child { border-bottom:none; }
  .excel-col { font-family:'DM Mono',monospace; font-weight:500; color:var(--sienna); }
  .excel-field { color:var(--body); }
  .excel-table { font-size:9px; color:var(--muted); letter-spacing:0.06em; }
  .excel-row.unused .excel-field { color:var(--muted); font-style:italic; }
  .excel-row.unused .excel-col { color:var(--muted); }

   @media (max-width: 860px) {
    .arch-grid { grid-template-columns: 1fr; }
    .arch-connector { padding-top: 0; transform: rotate(90deg); }
    .est-grid { grid-template-columns: 1fr; }
    .notes-grid { grid-template-columns: 1fr; }
    .total-bar { flex-direction: column; align-items: flex-start; }
    .total-equiv { text-align: left; }
  }
</style>
